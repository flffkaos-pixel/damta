{
  "name": "mood-play",
  "version": "2.0.0",
  "description": "💨 온라인 담타 — Cloudflare Pages + Workers + Durable Objects로 마이그레이션됨",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "deploy:cf": "wrangler pages deploy ./public",
    "deploy:cf:preview": "wrangler pages dev ./public"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
